You are going to create an “Add New Expense” widget that matches the provided screenshot exactly, with the following required modifications:

🔧 Required Changes

Remove the “Tags” field completely.

In the Amount field, replace the $ (dollar) symbol with DZD.

The Select Category section must be a horizontal sliding selector, identical to the behavior shown in the second screenshot.

For choosing the date, implement a calendar date-picker with a polished, modern UI.

The Save Expense button must have a background color matching the primary theme color of the app.

🗂 Backend Requirements

All fields must be connected to the backend.

When the user saves an expense, it must be stored locally in the system’s database.

Ensure clean state management and error handling for all fields.

🎯 Final Output

Deliver:

Complete UI code for the updated Add Expense widget

Backend integration steps

Local data-saving implementation

Any additional helper functions required

Make sure everything is fully functional, visually consistent with the screenshots, and polished.